namespace Carbon.Media.Metadata
{
    public enum SubjectDistanceRange : short
    {
        Unknown = 0,
        Macro   = 1,
        Close   = 2, // CloseView
        Distant = 3  // DistantView
    }
}

// tag: 41996