namespace Carbon.Media.Metadata
{
    public enum ExifSubjectDistanceRange : short
    {
        Unknown = 0,
        Macro   = 1,
        Close   = 2, // CloseView
        Distant = 3  // DistantView
    }
}

// tag: 41996