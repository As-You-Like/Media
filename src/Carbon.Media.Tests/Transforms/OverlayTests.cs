using Xunit;

namespace Carbon.Media.Drawing.Tests
{
    public class RectTests
	{
        /*
        [Fact]
        public void FromText()
        {
            var overlay = DrawRectangleCommand.Create(CallSyntax.Parse("rectangle(100,300,red,mode:burn,x:1,y:2,align:center,padding:10px)"));

            Assert.Equal("red", overlay.Fill);
            Assert.Equal(BlendMode.ColorBurn, overlay.BlendMode);
            Assert.Equal(1d, overlay.Box.X.Value);
            Assert.Equal(2d, overlay.Box.Y.Value);
            Assert.Equal(100d, overlay.Box.Width.Value);
            Assert.Equal(300d, overlay.Box.Height.Value);
            Assert.Equal(Alignment.Center, overlay.Align);
            Assert.Equal("10", overlay.Box.Padding.ToString());
        }
        */
	}
}